# No environment variables are required.
# All Firebase configuration and manual payment gateways (bKash / Nagad Send Money)
# are configured client-side and via Firestore without any external secrets or env vars.
